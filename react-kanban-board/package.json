{
  "name": "kanban-board",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://lucalanca.github.io/redux-talk",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "lodash.groupby": "^4.6.0",
    "lodash.sortby": "^4.7.0",
    "node-uuid": "^1.4.7",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.1",
    "react-router-redux": "^4.0.7",
    "redux-logger": "^2.7.4"
  },
  "scripts": {
    "copy-common": "cp -R ../common-kanban-redux ./src",
    "start": "npm run copy-common && react-scripts start",
    "build": "npm run copy-common && react-scripts start  react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
